# تعليمات الإعداد السريع - نظام إدارة النقل الجامعي

## 🚀 البدء السريع (5 دقائق)

### الخطوة 1: تثبيت المتطلبات
```bash
# تأكد من وجود Node.js 18+
node --version

# تأكد من وجود PostgreSQL
psql --version
```

### الخطوة 2: تثبيت الحزم
```bash
npm install
```

### الخطوة 3: إعداد قاعدة البيانات

#### أ. إنشاء قاعدة بيانات PostgreSQL
```bash
# افتح PostgreSQL
psql -U postgres

# أنشئ قاعدة البيانات
CREATE DATABASE university_transport;

# اخرج
\q
```

#### ب. إعداد ملف البيئة
```bash
# انسخ ملف المثال
cp .env.example .env

# عدّل ملف .env وضع رابط قاعدة البيانات
# DATABASE_URL="postgresql://postgres:password@localhost:5432/university_transport?schema=public"
```

**مهم**: استبدل `password` بكلمة المرور الخاصة بـ PostgreSQL

#### ج. إنشاء الجداول
```bash
# إنشاء جميع الجداول
npm run db:push

# إنشاء Prisma Client
npm run db:generate

# إضافة بيانات تجريبية (اختياري)
npm run db:seed
```

### الخطوة 4: تشغيل المشروع
```bash
# وضع التطوير
npm run dev

# افتح المتصفح على http://localhost:3000
```

---

## 🎯 الاستخدام السريع

### الصفحة الرئيسية
```
http://localhost:3000
```
- اختر "لوحة التحكم" لمتابعة الرحلات
- اختر "واجهة المندوب" لتسجيل رحلة جديدة

### واجهة المندوب
```
http://localhost:3000/delegate
```
1. اختر الرحلة الأساسية
2. حدد التاريخ والاتجاه (ذهاب/عودة)
3. اختر الوقت
4. أدخل عدد الطلاب
5. حدد الحالة
6. اضغط "تسجيل الرحلة"

### لوحة التحكم
```
http://localhost:3000/dashboard
```
- شاهد الإحصائيات الشاملة
- فلتر الرحلات حسب التاريخ والحالة
- عرض أفضل السائقين والجامعات

### لوحة الإدارة
```
http://localhost:3000/admin
```
- إدارة الجامعات والسائقين
- إدارة الباصات والمناديب
- إنشاء رحلات أساسية
- استيراد ملفات Excel

---

## 📥 استيراد البيانات من Excel

### تنسيق الملف المطلوب

يجب أن يحتوي ملف Excel على الأعمدة التالية:

| الجامعة | السائق | الباص | المندوب | عدد رحلات الذهاب | عدد رحلات العودة |
|---------|--------|-------|---------|-----------------|-----------------|
| جامعة الملك سعود | أحمد محمد | BUS-001 | فيصل العتيبي | 9 | 7 |

**أعمدة الأوقات للذهاب:**
- ذهاب_08:30
- ذهاب_09:30
- ذهاب_10:30
- ذهاب_11:30
- ذهاب_12:30
- ذهاب_13:30
- ذهاب_14:30
- ذهاب_15:30
- ذهاب_المجمّع

**أعمدة الأوقات للعودة:**
- عودة_12:30
- عودة_13:30
- عودة_14:30
- عودة_15:30
- عودة_16:30
- عودة_17:30
- عودة_المجمّع

**القيم في أعمدة الأوقات:**
- رقم = عدد الطلاب في هذه الرحلة
- فارغ = لا توجد رحلة في هذا الوقت

### طريقة الاستيراد
1. اذهب إلى `/admin/import`
2. اختر ملف Excel
3. اضغط "رفع واستيراد"
4. انتظر حتى تتم المعالجة
5. شاهد النتائج

---

## 🛠️ الأوامر المفيدة

```bash
# التطوير
npm run dev                # تشغيل وضع التطوير
npm run build             # بناء للإنتاج
npm start                 # تشغيل الإنتاج

# قاعدة البيانات
npm run db:push           # تحديث قاعدة البيانات
npm run db:generate       # توليد Prisma Client
npm run db:studio         # فتح Prisma Studio
npm run db:seed           # إضافة بيانات تجريبية

# الكود
npm run lint              # فحص الكود
```

---

## ⚠️ حل المشاكل الشائعة

### خطأ: لا يمكن الاتصال بقاعدة البيانات
**الحل:**
1. تأكد من تشغيل PostgreSQL
2. تأكد من صحة `DATABASE_URL` في `.env`
3. تأكد من وجود قاعدة البيانات:
```bash
psql -U postgres -c "\l"
```

### خطأ: Module not found
**الحل:**
```bash
# احذف المجلدات وأعد التثبيت
rm -rf node_modules package-lock.json
npm install
```

### خطأ: Prisma Client not generated
**الحل:**
```bash
npm run db:generate
```

### خطأ: Port 3000 already in use
**الحل:**
```bash
# استخدم منفذ آخر
PORT=3001 npm run dev
```

### خطأ في استيراد Excel
**الحل:**
1. تأكد من صحة أسماء الأعمدة (يجب أن تكون بالعربية)
2. تأكد من وجود القيم المطلوبة (الجامعة، السائق، الباص، المندوب)
3. جرب بملف صغير أولاً (سطر واحد)

---

## 📊 البيانات التجريبية

بعد تشغيل `npm run db:seed`، سيتم إضافة:
- 4 جامعات
- 5 سائقين
- 5 باصات
- 3 مناديب
- 4 رحلات أساسية
- رحلات يومية لتاريخ اليوم

---

## 🔐 الأمان (للإنتاج)

قبل النشر على الإنترنت:

1. **غيّر DATABASE_URL** في `.env` لقاعدة بيانات الإنتاج
2. **أضف نظام مصادقة** (NextAuth.js مثلاً)
3. **استخدم HTTPS** فقط
4. **راجع الصلاحيات** لقاعدة البيانات
5. **أضف Rate Limiting** للـ API
6. **فعّل CORS** بشكل صحيح

---

## 📱 الوصول من الموبايل

إذا أردت الوصول من هاتفك المحمول على نفس الشبكة:

1. اعرف IP جهازك:
```bash
# macOS/Linux
ifconfig | grep inet

# Windows
ipconfig
```

2. افتح المتصفح على الموبايل:
```
http://192.168.1.X:3000
```
(استبدل X برقم IP الخاص بك)

---

## 📞 الدعم

إذا واجهت مشكلة:
1. راجع ملف README.md الكامل
2. تحقق من سجلات الأخطاء في Terminal
3. افتح Prisma Studio لفحص قاعدة البيانات: `npm run db:studio`
4. تأكد من أن جميع الحزم مثبتة بشكل صحيح

---

**نصيحة**: ابدأ بتجربة البيانات التجريبية أولاً قبل إضافة بياناتك الحقيقية!

✅ **جاهز للاستخدام!** افتح http://localhost:3000 وابدأ الآن!

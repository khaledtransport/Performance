# 🚀 خطوات النشر والبدء

## 1️⃣ قبل البدء

تأكد من المتطلبات التالية:

```bash
✅ Node.js 18+ مثبت
✅ npm أو yarn مثبت
✅ قاعدة البيانات PostgreSQL أو Supabase جاهزة
✅ متغيرات البيئة محدثة في .env.local
```

---

## 2️⃣ إعداد المشروع

### خطوة 1: تثبيت الحزم

```bash
npm install
```

### خطوة 2: إعداد قاعدة البيانات

```bash
# تحديث schema في قاعدة البيانات
npx prisma migrate dev --name add-districts

# أو إذا كنت تستخدم Supabase مباشرة
npx prisma db push
```

### خطوة 3: تحميل البيانات التجريبية

```bash
npm run db:seed
```

### خطوة 4: تشغيل الخادم

```bash
npm run dev
```

الخادم سيعمل على: `http://localhost:3000`

---

## 3️⃣ التحقق من التثبيت

### افتح المتصفح وتحقق من:

1. **الصفحة الرئيسية:**

   ```
   http://localhost:3000
   ✅ تحقق من وجود "إدارة الأحياء" في الملاحة
   ```

2. **إدارة الأحياء:**

   ```
   http://localhost:3000/admin/districts
   ✅ يجب أن ترى 4 أحياء تجريبية
   ```

3. **إدارة الباصات:**

   ```
   http://localhost:3000/admin/buses
   ✅ كل باص يجب أن يظهر الحي المرتبط به
   ```

4. **تسجيل الرحلات:**
   ```
   http://localhost:3000/delegate
   ✅ الأوقات يجب أن تظهر بصيغة AM/PM
   ```

---

## 4️⃣ اختبار المميزات

### اختبار نظام الأحياء:

```
1. اذهب إلى /admin/districts
2. انقر "إضافة حي جديد"
3. أدخل:
   - الاسم: "حي الملقا"
   - الوصف: "منطقة سكنية حديثة"
4. انقر حفظ
5. تحقق من ظهور الحي الجديد
6. اختبر التعديل والحذف
```

### اختبار ربط الأحياء بالباصات:

```
1. اذهب إلى /admin/buses
2. انقر "إضافة باص جديد"
3. ملء البيانات:
   - رقم الباص: BUS-100
   - السعة: 50
   - رقم اللوحة: أ ب ج 1000
   - الحي: اختر "حي الملقا"
4. انقر حفظ
5. تحقق من ظهور الحي بجانب البيانات
```

### اختبار الأوقات الجديدة:

```
1. اذهب إلى /delegate
2. اختر رحلة أساسية
3. اختر اتجاه "ذهاب"
4. تحقق من ظهور الأوقات بصيغة:
   ✅ 7:30 AM
   ✅ 8:30 AM
   ✅ 1:30 PM
   ✅ 2:30 PM
5. اختر وقت وسجل رحلة
6. تحقق من حفظ البيانات
```

---

## 5️⃣ استيراد البيانات من Excel

إذا كنت تريد استيراد بيانات من ملف Excel:

```bash
1. اذهب إلى /admin
2. ابحث عن خيار استيراد
3. اختر الملف
4. تأكد من أن أسماء الأعمدة صحيحة
5. الأوقات ستُحفظ بصيغة AM/PM تلقائياً
```

---

## 6️⃣ النسخ الاحتياطي والصيانة

### قاعدة البيانات:

```bash
# عرض بيانات قاعدة البيانات
npx prisma studio

# ثم افتح: http://localhost:5555
```

### محو وإعادة تحميل البيانات التجريبية:

```bash
# حذف جميع البيانات
npx prisma db seed

# أو يدوياً
npx prisma migrate reset
```

---

## 7️⃣ نصائح مهمة

### ✅ الأمور التي يجب مراعاتها:

- لا تحذف قاعدة البيانات دون عمل نسخة احتياطية
- تحقق من متغيرات البيئة قبل النشر
- اختبر جميع العمليات قبل النشر الفعلي

### ⚠️ المشاكل الشائعة وحلولها:

**المشكلة:** Prisma client لم يتم تحديثه

```bash
الحل: npx prisma generate
```

**المشكلة:** قاعدة البيانات لا تحتوي على جداول جديدة

```bash
الحل: npx prisma db push
أو: npx prisma migrate dev
```

**المشكلة:** الأحياء لا تظهر

```bash
الحل:
1. تأكد من تشغيل seed: npm run db:seed
2. تحديث الصفحة: Ctrl+Shift+R
```

**المشكلة:** الأوقات تظهر بالصيغة القديمة

```bash
الحل:
1. حذف cache المتصفح
2. تحديث الصفحة
3. تحقق من كود delegate/page.tsx
```

---

## 8️⃣ الوثائق والموارد

الملفات المتاحة للمراجعة:

- 📄 `COMPLETION_REPORT.md` - ملخص التطويرات
- 📄 `IMPLEMENTATION_SUMMARY.md` - تفاصيل شاملة
- 📄 `QUICK_START.md` - دليل سريع
- 📄 `DESIGN_SYSTEM.md` - معايير التصميم

---

## 9️⃣ خطوات النشر (Deployment)

### للبيئة الإنتاجية:

```bash
# 1. بناء المشروع
npm run build

# 2. تشغيل الإصدار الإنتاجي
npm start

# 3. التحقق من الأخطاء
npm run lint
npm run type-check
```

### على Vercel:

```bash
# 1. ادفع الكود إلى GitHub
git push origin main

# 2. اربط Vercel مع المستودع
# Vercel سيقوم بالبناء والنشر تلقائياً

# 3. تعيين متغيرات البيئة في لوحة Vercel
DATABASE_URL=...
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```

---

## 🔟 الدعم والمساعدة

إذا واجهت مشاكل:

1. تحقق من السجلات: `npm run dev --verbose`
2. راجع التوثيق في المشروع
3. تحقق من ملف `.env.local`
4. تأكد من اتصالك بقاعدة البيانات

---

## ✅ قائمة التحقق قبل النشر

- [ ] تثبيت الحزم
- [ ] تحديث قاعدة البيانات
- [ ] تشغيل seed
- [ ] اختبار جميع الصفحات الجديدة
- [ ] التحقق من الأحياء والأوقات الجديدة
- [ ] اختبار API endpoints
- [ ] التحقق من عدم وجود أخطاء
- [ ] حفظ نسخة احتياطية من البيانات
- [ ] النشر على الخادم

---

## 🎉 تم!

الآن نظام إدارة النقل الجامعي محدث وجاهز للاستخدام مع الميزات الجديدة! 🚀

**الحالة:** ✅ جاهز للإطلاق
